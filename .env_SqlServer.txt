# Motor de base de datos: sqlserver | postgresql | mysql
DB_ENGINE=mysql

# Conexion
DB_SERVER=localhost
DB_PORT=3306
DB_DATABASE=PptoBD
DB_USER=root
DB_PASSWORD=
DB_SCHEMA=PptoBD

# Solo SQL Server (ignorado si usas mysql)
DB_DRIVER=ODBC Driver 17 for SQL Server
DB_WINDOWS_AUTH=False

# Flask
FLASK_PORT=5050

# Clave secreta para sesiones (genera con: python -c "import secrets; print(secrets.token_hex(32))")
APP_SECRET_KEY=
